


public class Demo 
	{
		public static void main(String arg[])
		{
			box box=new box();
			box.setBase(10.0);
			box.setHeight(15);
			
			double area;
			area=0.5 * (box.getBase()*box.getHeight());
			
			System.out.println("size is:"+area);
		}
}
